---
title: The site stands up
groups: 3
# Groups 2 and 3 touch different files and are independent of each other. They
# run in sequence anyway, because there is one working tree and it is the
# review surface.
order: [1, 2, 3]
---

One group at a time. Check an item only after its validation passes.

## 1. The site builds

Status: completed

- [x] Create `docs/package.json` with astro, `@astrojs/starlight` and sharp,
      and the scripts `dev`, `build` and `preview`.
- [x] Write `docs/astro.config.mjs`: the starlight integration, `site`, and
      `i18n` with `defaultLocale: 'en'`, locales `en` and `pt`, and no `root`.
- [x] Write `docs/tsconfig.json` and `docs/src/content.config.ts` declaring the
      starlight docs collection.
- [x] Add `docs/public/favicon.svg`.
- [x] Add a one line `docs/src/content/docs/en/index.mdx` and its `pt` twin, so
      the build has a page to render. The words arrive in group 2.
- [x] Add `docs/node_modules`, `docs/dist` and `docs/.astro` to `.gitignore`.
- [x] Add `docs/dist` and `docs/.astro` to `.prettierignore`.
- [x] Add the same three to the `ignores` of `.markdownlint-cli2.jsonc`, with
      one line saying they are build output.

Validation:

- `cd docs && npm install && npm run build` exits zero.
- `/en/` and `/pt/` are both in `docs/dist`, and `docs/dist/index.html` is not.
- `npm run check`, `npm run lint` and `npm run format:check` pass at the root,
  and the budget line the check prints is the same as before this group.

## 2. The home reads

Status: completed

- [x] Write `docs/src/content/docs/en/index.mdx` in full: the splash hero, the
      gate quoted alone, the install command with the degit alternative, the
      routing table from `README.md`, four cards, and what is not there.
- [x] Write `docs/src/content/docs/pt/index.mdx` as the same six sections. It
      is an adaptation and not a translation: same argument, same length.
- [x] Confirm no dash character entered either file, since the house rule
      governs prose the repository ships.

Validation:

- Both homes render the six sections in the order `change.md` gives.
- The gate reads as two quoted sentences with no heading or prose inside the
  quote.
- `npm run lint` passes on both files.

## 3. The structure is navigable

Status: not started

- [ ] Create the twenty one remaining pages under
      `docs/src/content/docs/en/`, one per row of the section table, each with
      a title, a description and an empty body.
- [ ] Create their `pt` twins under `docs/src/content/docs/pt/`, titled and
      described in Portuguese, bodies empty.
- [ ] Declare the eight sidebar groups in `docs/astro.config.mjs`, each
      autogenerated from its directory, with `translations` carrying the
      Portuguese label for every group.
- [ ] Read the built sidebar in both languages and confirm the page order
      inside each group is the order `change.md` gives, setting
      `sidebar.order` in the frontmatter where the default disagrees.

Validation:

- `cd docs && npm run build` exits zero with no warning about an unresolved
  sidebar entry.
- The sidebar shows eight groups in both languages, translated, and every entry
  in it opens a page that exists.
- `npm run lint` passes across `docs/`.
